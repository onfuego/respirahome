<section class="section">
  <div class="container container-narrow">
    <header class="section-head">
      <h1>{{ page.title }}</h1>
    </header>
    <div class="rte">
      {{ page.content }}
    </div>
  </div>
</section>
